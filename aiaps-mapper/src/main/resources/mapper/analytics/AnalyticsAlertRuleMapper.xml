<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiaps.mapper.analytics.AnalyticsAlertRuleMapper">

    <select id="selectActiveRules" resultType="com.aiaps.domain.analytics.AnalyticsAlertRule">
        SELECT *
        FROM analytics_alert_rule
        WHERE is_active = 1
    </select>

</mapper>
