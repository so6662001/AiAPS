<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiaps.mapper.base.BasMaterialMapper">
    <select id="selectByCategory" resultType="com.aiaps.domain.base.BasMaterial">
        SELECT * FROM bas_material WHERE category_code = #{categoryCode} AND is_active = 1
    </select>
    <select id="selectByCategoryAndSpec" resultType="com.aiaps.domain.base.BasMaterial">
        SELECT * FROM bas_material 
        WHERE category_code = #{categoryCode} AND is_active = 1
        <if test="widthMin != null"> AND width >= #{widthMin} </if>
        <if test="widthMax != null"> AND width &lt;= #{widthMax} </if>
        <if test="thicknessMin != null"> AND thickness >= #{thicknessMin} </if>
        <if test="thicknessMax != null"> AND thickness &lt;= #{thicknessMax} </if>
    </select>
</mapper>
