<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiaps.mapper.base.BasMixContractPolicyMapper">

    <select id="selectByCustomer" resultType="com.aiaps.domain.base.BasMixContractPolicy">
        SELECT * FROM bas_mix_contract_policy
        WHERE policy_scope = 'CUSTOMER'
          AND customer_code = #{customerCode}
          AND is_active = 1
        LIMIT 1
    </select>

    <select id="selectGlobal" resultType="com.aiaps.domain.base.BasMixContractPolicy">
        SELECT * FROM bas_mix_contract_policy
        WHERE policy_scope = 'GLOBAL'
          AND is_active = 1
        LIMIT 1
    </select>

</mapper>
