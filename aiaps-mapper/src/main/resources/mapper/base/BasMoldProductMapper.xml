<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiaps.mapper.base.BasMoldProductMapper">

    <select id="selectByMoldId" resultType="com.aiaps.domain.base.BasMoldProduct">
        SELECT *
        FROM bas_mold_product
        WHERE mold_id = #{moldId}
        ORDER BY is_primary DESC, efficiency_rate DESC
    </select>

    <select id="selectByMaterialId" resultType="com.aiaps.domain.base.BasMoldProduct">
        SELECT *
        FROM bas_mold_product
        WHERE PrdtID = #{prdtId}
        ORDER BY is_primary DESC, efficiency_rate DESC
    </select>

</mapper>
