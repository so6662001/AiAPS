<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiaps.mapper.base.BasWcProductRateMapper">
    <select id="selectRate" resultType="com.aiaps.domain.base.BasWcProductRate">
        SELECT * FROM bas_wc_product_rate
        WHERE wc_id = #{wcId} AND PrdtID = #{prdtId} AND is_active = 1
        LIMIT 1
    </select>
    <select id="selectByWcId" resultType="com.aiaps.domain.base.BasWcProductRate">
        SELECT * FROM bas_wc_product_rate
        WHERE wc_id = #{wcId} AND is_active = 1
    </select>
</mapper>
