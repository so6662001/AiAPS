<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiaps.mapper.demand.DemDemandLineMapper">

    <select id="selectOpenDemands" resultType="com.aiaps.domain.demand.DemDemandLine">
        SELECT *
        FROM dem_demand_line
        WHERE PrdtID = #{prdtId}
          AND line_status = 'OPEN'
          <if test="patName != null and patName != ''">
              AND PATName = #{patName}
          </if>
        ORDER BY required_date
    </select>

    <select id="selectByContract" resultType="com.aiaps.domain.demand.DemDemandLine">
        SELECT *
        FROM dem_demand_line
        WHERE contract_no = #{contractNo}
        ORDER BY line_no
    </select>

</mapper>
