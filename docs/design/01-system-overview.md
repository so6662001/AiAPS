# AiAPS — 钢铁行业 MRP 与排产自动化系统 · 总体设计

> 版本：1.0 | 最后更新：2026-03-17

---

## 1. 项目背景与目标

### 1.1 业务背景

本系统面向钢铁生产制造与加工行业，客户核心业务覆盖：

| 业务类型 | 典型工艺 | 特点 |
|---------|---------|------|
| 管材生产 | 焊管/无缝管：开卷 → 成型 → 焊接 → 定尺切割 → 探伤 → 包装 | 连续流生产，节拍式排产 |
| 型材生产 | 热轧/冷弯：加热 → 轧制 → 矫直 → 切割 → 打包 | 批次式生产，换规格换辊 |
| 货车车架/车梁 | 下料 → 成型 → 焊接 → 涂装 → 装配 | 离散制造，BOM 驱动 |
| 开平 | 开卷 → 矫平 → 定尺剪切 | 来料加工模式为主 |
| 剪切/折弯 | 剪板 → 折弯 → 成型 | 短工序，多品种小批量 |

### 1.2 三种生产驱动模式

```
┌─────────────────────────────────────────────────────────┐
│                    需求来源层                             │
│                                                         │
│  ┌──────────┐   ┌──────────┐   ┌──────────────┐        │
│  │ 订货合同  │   │ 期货订单  │   │  库存备货     │        │
│  │ (MTO)    │   │ (MTS预测) │   │  (Safety     │        │
│  │          │   │          │   │   Stock)     │        │
│  └────┬─────┘   └────┬─────┘   └──────┬───────┘        │
│       │              │                │                 │
│       └──────────────┼────────────────┘                 │
│                      ▼                                  │
│            ┌──────────────────┐                         │
│            │  MRP 需求合并引擎  │                         │
│            └────────┬─────────┘                         │
│                     ▼                                   │
│            ┌──────────────────┐                         │
│            │   排产调度引擎     │                         │
│            └──────────────────┘                         │
└─────────────────────────────────────────────────────────┘
```

- **订货合同 (MTO — Make to Order)**：以销售合同为需求源头，交期明确，产品规格确定，排产优先级高。
- **期货订单 (MTS — Make to Stock/预测型)**：根据市场行情预判，提前备料生产，交期有一定弹性。
- **库存备货 (Safety Stock)**：根据安全库存水位线自动触发补货生产，属于被动式需求。

### 1.3 系统目标

| 目标 | 量化指标 |
|------|---------|
| 自动化程度 | MRP 计算全自动，排产结果自动生成，人工干预 < 20% |
| 排产调整效率 | 已排产计划的调整操作 ≤ 3 步完成（拖拽 + 确认） |
| 并发支持 | ≥ 500 人同时在线操作 |
| MRP 计算性能 | 10,000 条需求行在 30 秒内完成展算 |
| 排产可视化 | 甘特图实时展示，支持多维度（产线/订单/物料）视图 |

### 1.4 技术约束

| 项目 | 选型 | 说明 |
|-----|------|------|
| 后端语言 | Java 8+ (Spring Boot) | 主体业务逻辑 |
| 数据库 | SQL Server 2008 | 需兼容 2008 语法，不使用高版本专有特性 |
| 前端框架 | Vue 3 + Element Plus | 配合甘特图组件 |
| 并发目标 | 500 人在线 | 重读轻写场景 |

---

## 2. 系统整体架构

### 2.1 分层架构

```
┌────────────────────────────────────────────────────────────────┐
│                        前端展示层 (Vue 3)                       │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────────────┐ │
│  │ MRP 工作台│ │排产甘特图 │ │ 物料看板   │ │ 产能/负荷监控    │ │
│  └──────────┘ └──────────┘ └───────────┘ └──────────────────┘ │
├────────────────────────────────────────────────────────────────┤
│                        API 网关层                              │
│              (Nginx + Spring Cloud Gateway)                    │
├────────────────────────────────────────────────────────────────┤
│                       业务服务层 (Spring Boot)                  │
│  ┌────────────┐ ┌────────────┐ ┌─────────────┐ ┌───────────┐ │
│  │ 需求管理    │ │ MRP 引擎   │ │ 排产调度引擎  │ │ 基础数据   │ │
│  │ 服务       │ │ 服务       │ │  服务        │ │ 服务      │ │
│  └────────────┘ └────────────┘ └─────────────┘ └───────────┘ │
│  ┌────────────┐ ┌────────────┐ ┌─────────────┐               │
│  │ 库存管理    │ │ 采购建议    │ │ 报工/实绩    │               │
│  │ 服务       │ │ 服务       │ │  服务        │               │
│  └────────────┘ └────────────┘ └─────────────┘               │
├────────────────────────────────────────────────────────────────┤
│                     公共基础设施层                              │
│  ┌────────┐ ┌────────┐ ┌────────────┐ ┌────────┐ ┌────────┐ │
│  │Redis   │ │消息队列 │ │ 任务调度    │ │日志中心 │ │权限认证 │ │
│  │缓存    │ │(可选)   │ │(Quartz)   │ │        │ │(JWT)   │ │
│  └────────┘ └────────┘ └────────────┘ └────────┘ └────────┘ │
├────────────────────────────────────────────────────────────────┤
│                     数据持久层                                 │
│              SQL Server 2008 (主库 + 只读副本)                  │
└────────────────────────────────────────────────────────────────┘
```

### 2.2 500 人并发性能设计要点

| 策略 | 具体方案 |
|------|---------|
| 读写分离 | SQL Server 2008 数据库镜像/日志传送实现只读副本，查询走副本 |
| 多级缓存 | BOM、工艺路线、工作中心等基础数据 → Redis 缓存（TTL 30min） |
| 异步计算 | MRP 展算、排产计算提交后台线程池异步执行，前端轮询/WebSocket 推送进度 |
| 分页加载 | 所有列表接口强制分页，单页 ≤ 200 条 |
| 连接池 | HikariCP，最大连接数 60~80，配合 SQL Server 连接池 |
| 前端优化 | 甘特图虚拟滚动、按需渲染；大列表使用 virtual-scroll |

---

## 3. 核心业务模块概览

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   ┌────────────┐     ┌────────────┐     ┌────────────────────┐  │
│   │  基础数据    │     │  需求管理   │     │   库存管理          │  │
│   │            │     │            │     │                    │  │
│   │ · 物料主数据 │     │ · 销售合同  │     │ · 即时库存          │  │
│   │ · BOM      │     │ · 期货订单  │     │ · 在途库存（采购中）  │  │
│   │ · 工艺路线  │     │ · 备货计划  │     │ · 在制库存（生产中）  │  │
│   │ · 工作中心  │     │ · 需求预测  │     │ · 安全库存          │  │
│   │ · 产线/设备 │     │            │     │ · 可用量计算         │  │
│   │ · 工厂日历  │     │            │     │                    │  │
│   └─────┬──────┘     └─────┬──────┘     └──────────┬─────────┘  │
│         │                  │                       │             │
│         └──────────────────┼───────────────────────┘             │
│                            ▼                                     │
│                   ┌────────────────┐                              │
│                   │   MRP 展算引擎  │                              │
│                   │                │                              │
│                   │ · 毛需求计算    │                              │
│                   │ · 净需求计算    │                              │
│                   │ · 批量规则     │                              │
│                   │ · 提前期偏移    │                              │
│                   │ · 计划订单生成  │                              │
│                   └───────┬────────┘                              │
│                           │                                      │
│              ┌────────────┼────────────┐                         │
│              ▼            ▼            ▼                         │
│     ┌────────────┐ ┌───────────┐ ┌──────────┐                   │
│     │ 生产计划单  │ │ 采购建议单 │ │ 委外加工单│                   │
│     └─────┬──────┘ └───────────┘ └──────────┘                   │
│           │                                                      │
│           ▼                                                      │
│   ┌───────────────────┐                                          │
│   │   排产调度引擎      │                                          │
│   │                   │                                          │
│   │ · 有限产能排产     │                                          │
│   │ · 多约束调度       │                                          │
│   │ · 甘特图可视化     │                                          │
│   │ · 拖拽调整/插单    │                                          │
│   │ · 冲突检测与解决   │                                          │
│   └───────────────────┘                                          │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 3.1 模块清单

| 序号 | 模块 | 核心职责 | 详细设计文档 |
|-----|------|---------|------------|
| M1 | 基础数据管理 | 物料、BOM、工艺路线、工作中心、工厂日历 | 02-data-model.md |
| M2 | 需求管理 | 三种来源的需求接入与合并 | 02-data-model.md |
| M3 | 库存管理 | 多维库存(即时/在途/在制/安全)可用量计算 | 02-data-model.md |
| M4 | MRP 展算引擎 | 净需求计算、计划订单生成、采购建议 | 03-mrp-engine.md |
| M5 | 排产调度引擎 | 有限产能排产、多约束调度、冲突检测 | 04-scheduling-engine.md |
| M6 | 排产可视化与调整 | 甘特图、拖拽、插单、锁定 | 05-frontend-design.md |
| M7 | 报工与实绩 | 实际生产进度反馈，驱动重排产 | 02-data-model.md |
| M8 | 系统管理 | 权限、日志、系统参数 | — |

---

## 4. 钢铁行业特有设计考量

### 4.1 物料特征

钢铁行业的物料管理有其独特性：

```
物料编码规则建议：
  品类(2位) + 材质(4位) + 规格(变长) + 表面处理(2位)
  
  示例：
  GC-Q235B-Φ89×4.0×6000-HG    (管材-Q235B-规格-镀锌)
  XC-Q345B-H200×100×5.5-RM    (型材-Q345B-H型钢规格-热轧)
  PB-Q235B-6.0×1500×6000-KP   (板材-Q235B-厚×宽×长-开平)
```

### 4.2 钢铁行业 BOM 特点

| 特点 | 说明 | 系统应对 |
|-----|------|---------|
| 原料可替代 | 同材质不同规格的钢卷/钢坯可替代 | 支持替代 BOM / 替代物料组 |
| 损耗率高 | 切割/焊接存在固定+比例损耗 | BOM 用量 = 净量 × (1 + 损耗率) |
| 联产品 | 开平/分条产生余料可再利用 | 支持联产品/副产品入库 |
| 规格驱动 | 同产品不同规格 BOM 差异大 | 支持配置 BOM (可变参数) |

### 4.3 产能建模

```
工作中心层次：

  工厂
   └── 车间
        └── 产线
             └── 工作中心 (工位/机台)
                  └── 设备

产能单位：
  - 连续型产线：吨/小时, 吨/班次
  - 离散型工位：件/小时, 工时/件
  - 切割设备：  刀次/小时, 米/小时
```

### 4.4 工厂日历与排班

```
排班模式：
  ├── 三班两倒 (8h × 3)    ← 热轧、焊管等连续生产
  ├── 两班倒   (12h × 2)   ← 冷弯、开平
  └── 常白班   (8h)        ← 折弯、剪切等离散工序

日历元素：
  ├── 工作日 / 休息日
  ├── 计划检修日
  ├── 换辊/换模时间 (setup time)
  └── 产线大修周期
```

---

## 5. 系统演进总结（V1.0→V3.0）

### 5.1 字段命名规范

系统全部字段对齐客户实际系统命名：

| 设计概念 | 实际字段名 | 说明 |
|---------|-----------|------|
| 物料ID | PrdtID | 物料主键 |
| 物料编号 | PrdtNo | 物料编码（品类+规格，不含材质/产地/长度） |
| 物料名称 | PrdtName | 物料名称 |
| 材质 | PATName / PrdtAttsID | 材质名称 / 材质ID |
| 产地 | PAName / PAID | 产地名称 / 产地ID |
| 资源号 | ResNo | 钢卷/带钢的卷号 |
| 批次号 | CardNo | 卡号 = 批次号 |
| 捆包号 | BindNo | 每件货物的唯一标识 |
| 单支条码 | ItemBarcode | 每支管的唯一条码（品类可控） |
| 备注1/2 | CardRemark / CardRemark2 | 批次备注 |
| 入库日期 | EnterDate | 入库日期 |

### 5.2 三端架构

系统采用三端架构：

| 端 | 技术栈 | 用途 |
|---|--------|------|
| Java后端 | Spring Boot 2.7 + MyBatis-Plus + SQL Server 2008 | 全部业务逻辑、MRP引擎、排产引擎 |
| Vue3前端 | Vite + Vue3 + TypeScript + Element Plus + ECharts | PC端管理界面(15个页面)，科技风UI |
| Android终端 | Java + Retrofit2 + ZXing | 车间上料扫码、生产报工、入库确认 |

### 5.3 核心设计决策总结

| 决策 | 说明 |
|------|------|
| 物料=品类+规格 | 材质和产地从物料剥离，作为库存/需求/排产的属性维度 |
| 长度=批次属性 | 长度不在物料编码中，作为库存批次属性，展示时动态拼接 |
| 重量为核心计量 | MRP净需求、成材率、产能全部按重量(吨)计算 |
| 品类BOM+公式 | 钢材无规格级BOM，通过品类BOM+规格推算公式计算原料 |
| 双轨LLC | 品类BOM和离散BOM合并计算低阶码 |
| 合同号贯穿 | contract_no 从需求到库存贯穿10张核心表 |
| 三级追溯 | 卡号→捆包号→单支条码，品类可控是否启用单支条码 |
| 多阶段排产 | 原料层(备料/分剪)→制造层(产线排产)，以罩退为瓶颈锚点 |
| 联合工序 | 酸轧联合(PLTCM)一条线，中间不出卷不入库 |
